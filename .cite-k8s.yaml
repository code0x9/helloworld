apiVersion: v1
kind: ReplicationController
metadata:
  name: test2-dev-controller
spec:
  replicas: 1
  selector:
    app: test2
  template:
    metadata:
      labels:
        app: test2
        phase: develop
    spec:
      containers:
      - name: test2
        image: docker-reg.krane.iwilab.com/cite/test1-test2:latest
        ports:
        - containerPort: 8080
        livenessProbe:
          # an http probe
          httpGet:
            path: /_status
            port: 8080
          # length of time to wait for a pod to initialize
          # after pod startup, before applying health checking
          initialDelaySeconds: 5
          timeoutSeconds: 1
---
apiVersion: v1
kind: Service
metadata:
  name: test2-dev-service
spec:
  selector:
    app: test2
    phase: develop
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080